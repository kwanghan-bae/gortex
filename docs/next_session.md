# Next Session

## 세션 목표
- **Intelligent Model Selection**: `EfficiencyMonitor`에 쌓인 실제 성능 데이터를 분석하여, 작업의 복잡도와 모델의 과거 성공률에 따라 가장 비용 효율적인 모델을 자동 할당하는 라우팅 엔진을 고도화한다.
- **Visual Evolution History**: 시스템이 스스로 어떻게 진화해왔는지(코드 변경 이력 및 성능 향상폭)를 웹 대시보드에서 직관적으로 확인할 수 있는 진화 히스토리 뷰를 구현한다.

## 컨텍스트
- 자가 진화 엔진이 구축되었으나, 아직은 데이터 기반의 지능적 판단보다는 규칙 기반의 동작이 강합니다.
- `EfficiencyMonitor`에 데이터가 축적되고 있으므로, 이를 활용한 통계적 의사결정이 가능해졌습니다.

## 범위 (Scope)
### 수행할 작업 (Do)
- `utils/efficiency_monitor.py`: 모델별 'Score Card'를 생성하는 분석 로직 추가.
- `agents/manager.py`: Score Card 정보를 바탕으로 `assigned_model`을 결정하는 지능형 라우팅 로직 보강.
- `ui/evolution_view.py` (신규): 진화 이력을 시각화하는 웹 컴포넌트 추가.

### 수행하지 않을 작업 (Do NOT)
- LLM 없이 작동하는 휴리스틱한 라우팅만으로 고정하지 않는다. (LLM의 유연한 판단력 활용)

## 기대 결과
- API 비용을 최소화하면서도 작업 성공률을 극대화하는 '스마트 리소스 배분' 달성.
- 사용자가 시스템의 자가 발전 과정을 시각적으로 신뢰할 수 있는 인터페이스 제공.

## 완료 기준
- 모델 성능 기반 자동 선택 로직 동작 확인.
- 웹 대시보드 내 진화 히스토리 섹션 추가.
- `docs/sessions/session_0066.md` 기록.