# Next Session

## 세션 목표
- **Autonomous Test Proliferation**: 자가 진화로 변경된 코드나 신규 기능에 대해 `Analyst`와 `Coder`가 협력하여 유닛 테스트를 자동으로 생성하고 커버리지를 끌어올리는 자가 증식 테스트 루프를 가동한다.
- **Architecture Drift Guard**: `Analyst`가 정기적으로 코드 관계도를 분석하여, 진화 과정에서 발생하는 '스파게티화' 또는 '의존성 꼬임'을 감지하고 아키텍처 가이드라인을 동적으로 갱신한다.

## 컨텍스트
- 시스템이 스스로 코드를 고치기 시작했으므로, 그 결과물을 검증할 '테스트' 역시 스스로 늘려나가야 무결성이 유지됩니다.
- 자율적인 수정이 누적될수록 아키텍처 일관성이 깨질 위험이 있으므로 이에 대한 자동 감시가 필요합니다.

## 범위 (Scope)
### 수행할 작업 (Do)
- `agents/analyst/base.py`: 미구현된 테스트 케이스를 찾아내고 생성 작업을 `Planner`에게 제안하는 로직 추가.
- `agents/coder.py`: 테스트 코드 전용 생성 프롬프트 및 검증 로직 강화.
- `utils/indexer.py`: 코드 간 의존성 그래프를 추출하여 아키텍처 왜곡을 분석하는 유틸리티 메서드 추가.

### 수행하지 않을 작업 (Do NOT)
- 기존에 잘 작동하는 테스트 코드를 임의로 삭제하지 않는다.

## 기대 결과
- 코드 변경과 동시에 테스트가 자동으로 따라오는 '자기 보호형' 개발 환경 구축.
- 시스템의 대규모 변경 중에도 아키텍처 무결성 유지.

## 완료 기준
- 신규 생성된 파일에 대해 테스트 파일이 자동으로 생성되는지 확인.
- 전체 코드 커버리지 수치 향상.
- `docs/sessions/session_0068.md` 기록.