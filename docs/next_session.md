# Next Session

## 세션 목표
- **Temporary Persona Creation (Persona Cloning)**: `Manager`가 매우 특수한 상황(예: 심각한 보안 이슈, 복잡한 신규 프레임워크 도입)을 감지했을 때, 기존 정의된 페르소나를 넘어 해당 작업에 최적화된 '임시 가상 페르소나'를 스스로 설계하고 투입하는 실험적 루프를 가동한다.
- **Architecture Health Dashboard**: `SynapticIndexer`의 지능 지수와 `Analyst`의 드리프트 위반 사항을 종합하여, 시스템 전체의 '아키텍처 건강도(Health Score)' 추이를 그래프로 시각화하는 웹 UI 기능을 구현한다.

## 컨텍스트
- 성격 튜닝이 성공적으로 안착됨에 따라, 이제는 상황에 맞는 성격을 '창조'할 수 있는 유연성이 필요합니다.
- 자가 진화의 성과를 객관적 수치(Health Score)로 증명함으로써 진화의 투명성을 높여야 합니다.

## 범위 (Scope)
### 수행할 작업 (Do)
- `agents/manager.py`: 특수 상황 감지 시 `assigned_persona`에 커스텀 지침을 즉석에서 생성해 주입하는 로직 추가.
- `ui/web_server.py`: 아키텍처 건강도 데이터를 제공하는 신규 API 엔드포인트 구현.
- `utils/indexer.py`: 복잡도, 중복도, 위반 건수를 종합한 `calculate_health_score` 메서드 추가.

### 수행하지 않을 작업 (Do NOT)
- 창조된 임시 페르소나가 영구적인 핵심 정책을 사용자 승인 없이 덮어쓰지 않도록 격리한다.

## 기대 결과
- 무한한 상황에 대응할 수 있는 '무한 페르소나' 체계의 초석 마련.
- 시스템의 건강 상태를 실시간으로 모니터링하고 시각화하여 진화의 방향성 타당성 확보.

## 완료 기준
- 상황에 따라 동적으로 생성된 페르소나 지침이 에이전트 호출 시 포함되는지 확인.
- 웹 대시보드에서 아키텍처 건강도 점수가 렌더링되는지 검증.
- `docs/sessions/session_0076.md` 기록.