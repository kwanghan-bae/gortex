# ⏭️ Gortex Next Session Context

**Date:** 2024-12-20
**Status:** Metaverse Dashboard & 3D Spatial Mapping Complete (v2.1.2)

## 🧠 Current Context
3D 지식 매핑 엔진이 구축되었습니다. 이제 Gortex는 방대한 프로젝트 지식과 사고 과정을 3D 공간 데이터로 변환하여 제공하며, 이는 웹 대시보드에서 입체적인 추론 탐색을 가능하게 하는 혁신적인 인터페이스의 기초가 됩니다.

## 🎯 Next Objective
**Agent Factory (Self-Spawning Nodes)**
1. **`Node Generation`**: 특정 분야의 전문 지식이 반복적으로 필요하다고 판단될 경우, `Manager`가 해당 분야에 특화된 새로운 에이전트 노드(예: `SecurityAuditAgent`, `DatabaseOptimAgent`)를 동적으로 생성합니다.
2. **`Code Spawning`**: `Coder`를 통해 새로운 노드의 파이썬 코드를 작성하고, `core/graph.py`에 해당 노드를 자동으로 등록하여 시스템의 기능을 실시간으로 확장하는 자가 증식 메커니즘을 구현합니다.

## 💬 Prompt for Next Agent
```text
@docs/gortex/SPEC.md 를 읽고 다음 작업을 이어나가.
현재 상태:
- 3D 지식 매핑 및 스트리밍 완료 (v2.1.2).
- 다음 목표: 자가 증식형 에이전트 생성기(Agent Factory).

작업 목표:
1. `agents/manager.py`에서 새로운 전문 에이전트가 필요함을 감지하고 `AgentFactory` 작업을 지시하는 로직을 보강해줘.
2. `core/graph.py`를 정적으로 분석하여 새로운 노드를 안전하게 삽입하고 워크플로우를 갱신하는 `SelfModifier` 기능을 구현해줘.
```